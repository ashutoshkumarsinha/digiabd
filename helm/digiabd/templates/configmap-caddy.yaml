{{- if .Values.caddy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "digiabd.fullname" . }}-caddy
  labels:
    {{- include "digiabd.labels" . | nindent 4 }}
data:
  Caddyfile: |
{{ tpl .Values.caddy.config . | indent 4 }}
{{- end }}
