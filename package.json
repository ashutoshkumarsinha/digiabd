{
  "name": "digiabd",
  "version": "0.1.0",
  "private": true,
  "description": "Digital As-Built Documentation platform for OFC networks",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "cp -n .env.example .env || true && npm install",
    "dev": "npm run dev -w @digiabd/api",
    "dev:web": "npm run dev -w @digiabd/web",
    "dev:mobile": "npm run start -w @digiabd/mobile",
    "build": "npm run build -w @digiabd/api && npm run build -w @digiabd/web",
    "build:api": "npm run build -w @digiabd/api",
    "build:web": "npm run build -w @digiabd/web",
    "test": "npm run test -w @digiabd/api",
    "test:api": "npm run test -w @digiabd/api",
    "lint": "npm run lint -w @digiabd/api",
    "lint:api": "npm run lint -w @digiabd/api",
    "db:up": "docker compose up -d postgres redis minio minio-init redpanda",
    "db:migrate": "npm run migrate && npm run db:migrate:phase2 && npm run db:migrate:phase3 && npm run db:migrate:phase4",
    "keycloak:up": "docker compose up -d keycloak keycloak-db",
    "keycloak:down": "docker compose stop keycloak keycloak-db",
    "observability:up": "docker compose up -d signoz",
    "observability:down": "docker compose stop signoz",
    "db:migrate:phase2": "docker compose exec -T postgres psql -U digiabd -d digiabd -f /docker-entrypoint-initdb.d/002_phase2.sql",
    "db:migrate:phase3": "docker compose exec -T postgres psql -U digiabd -d digiabd -f /docker-entrypoint-initdb.d/003_phase3_resilience.sql",
    "db:migrate:phase4": "docker compose exec -T postgres psql -U digiabd -d digiabd -f /docker-entrypoint-initdb.d/004_phase4_governance.sql",
    "db:down": "docker compose down",
    "stack:up": "docker compose up -d",
    "stack:down": "docker compose down",
    "proxy:up": "docker compose up -d caddy",
    "proxy:down": "docker compose stop caddy",
    "images:build": "packer init . && packer build -var \"version=dev\" .",
    "helm:lint": "helm lint ./helm/digiabd",
    "helm:template": "helm template digiabd ./helm/digiabd -n digiabd",
    "helm:install": "helm upgrade --install digiabd ./helm/digiabd -n digiabd --create-namespace",
    "helm:uninstall": "helm uninstall digiabd -n digiabd",
    "smoke": "bash scripts/smoke-test.sh",
    "smoke:phase2": "bash scripts/smoke-test-phase2.sh",
    "smoke:phase3": "bash scripts/smoke-test-phase3.sh",
    "smoke:phase4": "bash scripts/smoke-test-phase4.sh",
    "migrate": "docker compose exec -T postgres psql -U digiabd -d digiabd -f /docker-entrypoint-initdb.d/001_initial_schema.sql"
  },
  "engines": {
    "node": ">=20"
  }
}
